<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:parentTag="android.widget.LinearLayout">

    <!-- Progress Container -->
    <LinearLayout
        android:id="@+id/progress_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/spacing_medium">

        <!-- Linear Progress Indicator -->
        <com.google.android.material.progressindicator.LinearProgressIndicator
            android:id="@+id/progress_indicator"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:indeterminate="false"
            app:trackThickness="@dimen/progress_track_thickness"
            app:indicatorSize="@dimen/progress_indicator_size"
            tools:progress="75" />

        <!-- Progress Info Container -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_small"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Percentage Text -->
            <TextView
                android:id="@+id/percentage_text"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="@dimen/text_body"
                android:textColor="@color/on_surface"
                android:textStyle="bold"
                tools:text="75%" />

            <!-- Status Text -->
            <TextView
                android:id="@+id/status_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_caption"
                android:textColor="@color/on_surface_variant"
                tools:text="Processing files..." />

        </LinearLayout>

    </LinearLayout>

    <!-- Circular Progress Container (for alternative style) -->
    <LinearLayout
        android:id="@+id/progress_container_circular"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/spacing_medium"
        android:gravity="center_vertical"
        android:visibility="gone"
        tools:visibility="visible">

        <!-- Circular Progress Indicator -->
        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/circular_progress_indicator"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="@dimen/spacing_medium"
            android:indeterminate="false"
            app:trackThickness="@dimen/progress_track_thickness"
            app:indicatorSize="48dp"
            tools:progress="75" />

        <!-- Progress Info -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Percentage Text -->
            <TextView
                android:id="@+id/percentage_text_circular"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_title"
                android:textColor="@color/on_surface"
                android:textStyle="bold"
                tools:text="75%" />

            <!-- Status Text -->
            <TextView
                android:id="@+id/status_text_circular"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_caption"
                android:textColor="@color/on_surface_variant"
                tools:text="Processing files..." />

        </LinearLayout>

    </LinearLayout>

</merge> 